I need to develop a subclass of `DependenceAnalysis`. Figure out what the `kill`, `gen`, `in`, `out` sets are.

Profiling code perhaps?2

The first analysis I want to do is I want to be able to detect memory overlap and suggest using private copes if both threads only read and not write to memory.